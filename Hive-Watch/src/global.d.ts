declare global {
  namespace shaka {
    // Add any necessary interfaces or types for Shaka Player here (optional)

    // For example:

    export interface Player {
      // ... Player interface definition ...
    } // ... other interfaces and types ...
  }
}
